import ManagerLayout from "src/layouts/manager/ManagerLayout";

//분양관리
const ParcelOutList = () =>{
  return (
    <>

    </>
  )
}
ParcelOutList.getLayout = (page) => <ManagerLayout>{page}</ManagerLayout>;
export default ParcelOutList
